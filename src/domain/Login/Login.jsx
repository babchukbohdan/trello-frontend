import React from 'react';
import PropTypes from 'prop-types';

const Login = (props) => (
  <div className="login">
    <input type="text" name="" id="" />
    <input type="text" name="" id="" />
  </div>
);

Login.propTypes = {};

export default Login;
